(module hello-sdl
  (import :c "SDL2/SDL.h")
  (import :c "SDL2/SDL_video.h")
  (import :c "stdio.h"))

(fn main i32 ()
  (if (< 0 (SDL_Init 0x00000020u)) ; for SDL_INIT_VIDEO (we don't have even automatic upcasts yet)
       (puts "Failed to initialize"))
  (var event SDL_Event)
  (var window (ptr SDL_Window)
    (SDL_CreateWindow "Hello from Fulcrum"
                       500 ; pos x
                       500 ; pos y
                       800 ; width
                       600 ; height
                       0u))
  (SDL_ShowWindow window)
  (while (> (SDL_PollEvent (addr-of event)) 0)
    (SDL_Delay 100u))
  (SDL_DestroyWindow window)
  (SDL_Quit)
  (return 0))
