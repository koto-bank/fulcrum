(module if
  (import :c "stdio.h"))

(fn main i32 ((argc i32) (argv (array str 10)))
  (var i u32 1u)
  (puts "0th arg: ")
  (puts (nth argv 0))
  (while (< i (cast argc u32))
    (puts "Current arg is")
    (puts (nth argv i))
    (set i (+ i 1u)))
  (return 0))
