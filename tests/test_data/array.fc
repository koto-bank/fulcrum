(module array
  (import :c "stdio.h"))

(fn print-array void ((array (array str 10)) (size i32))
  (var i i32 0)
  (printf "Addr of passed array: %p\n" array)
  (while (< i size)
    (puts (nth array i))
    (set i (+ i 1))))

(fn main i32 ((argc i32) (argv (ptr str)))
  (var str-arr (array str 10))
  (var i i32 0)
  (while (and (< i argc) (< i 10))
    (set (nth str-arr i) (deref (ptr+ argv i)))
    (set i (+ i 1)))

  (set i 0)
  (while (< i argc)
    (puts (nth str-arr i))
    (set i (+ i 1)))

  (printf "Addr of array: %p addr of 1st elt: %p\n" str-arr (nth str-arr 0))
  (puts "----------------")

  (print-array str-arr argc)

  (return 0))
